# 面向移动端的高性能低光照图像增强系统 - 实现方案

## 一、项目定位

### 1.1 目标

在 Android 手机上实现实时低光照图像增强，展示从算法训练到端侧部署的全链路能力。

### 1.2 核心卖点

| 维度 | 展示的能力 |
|-----|-----------|
| 算法 | 理解 RepVGG 重参数化原理，能完成模型训练和导出 |
| 工程 | C++ 并行编程、内存管理、性能优化 |
| 部署 | NCNN 量化、移动端适配、真机实测 |

### 1.3 工作量分配

```
算法层 30% ─────────┐
                    ├──→ 一个完整的端到端项目
工程层 60% ─────────┤
                    │
验证层 10% ─────────┘
```

---

## 二、技术架构

### 2.1 整体流程

```
【训练阶段 - Python/PyTorch】

    LOL 数据集
         │
         ▼
    ┌─────────┐      ┌─────────┐      ┌─────────┐
    │ 低光图  │ ───→ │ RepVGG  │ ───→ │ 增强图  │
    │  输入   │      │  U-Net  │      │  输出   │
    └─────────┘      └─────────┘      └─────────┘
                          │
                     L1 Loss +
                   Perceptual Loss
                          │
                          ▼
                    model.pth
                          │
                   switch_to_deploy()  ← 重参数化：多分支→单分支
                          │
                          ▼
                    model.onnx


【部署阶段 - C++/NCNN】

                    model.onnx
                          │
                     onnx-simplifier
                          │
                     onnx2ncnn
                          │
                          ▼
                 ┌────────────────┐
                 │  model.param   │
                 │  model.bin     │
                 └────────────────┘
                          │
                     ncnnoptimize (FP16/INT8)
                          │
                          ▼
                 ┌────────────────┐
                 │ 量化后的模型    │
                 └────────────────┘


【推理阶段 - C++ 高性能引擎】

    输入大图 (4K/12MP)
         │
         ▼
    ┌─────────────────────────────────────┐
    │           Tiling 模块               │
    │  ┌─────┬─────┬─────┬─────┐         │
    │  │ T0  │ T1  │ T2  │ T3  │  切块    │
    │  ├─────┼─────┼─────┼─────┤ (带overlap)│
    │  │ T4  │ T5  │ T6  │ T7  │         │
    │  └─────┴─────┴─────┴─────┘         │
    └─────────────────────────────────────┘
                   │
                   ▼
    ┌─────────────────────────────────────┐
    │           线程池并行推理             │
    │                                     │
    │   Thread 0 ──→ 处理 T0, T4         │
    │   Thread 1 ──→ 处理 T1, T5         │
    │   Thread 2 ──→ 处理 T2, T6         │
    │   Thread 3 ──→ 处理 T3, T7         │
    │                                     │
    └─────────────────────────────────────┘
                   │
                   ▼
    ┌─────────────────────────────────────┐
    │           Merge 模块                │
    │                                     │
    │   Overlap 区域线性加权融合           │
    │   消除拼接接缝                       │
    │                                     │
    └─────────────────────────────────────┘
                   │
                   ▼
            输出增强大图
```

---

## 三、各模块详细设计

### 3.1 模型训练（算法层）

#### 网络结构

```
选择：U-Net + RepVGG Block

为什么用 RepVGG：
├─ 训练时：3x3 Conv + 1x1 Conv + Identity 三分支，提取更丰富特征
├─ 推理时：合并为单个 3x3 Conv，速度快
└─ 面试时：能讲"结构重参数化"，展示你懂模型优化

模型规模控制：
├─ 使用 RepVGG-A0 级别或更小
├─ Encoder: 4 层下采样
├─ Decoder: 4 层上采样
└─ 目标参数量: < 2M
```

#### 损失函数

```python
# 简单有效的组合
total_loss = L1_loss + 0.1 * perceptual_loss

# L1 Loss: 像素级重建
# Perceptual Loss: VGG 特征匹配，提升视觉质量
```

#### 数据集

```
LOL Dataset (Low-Light Dataset) - 用于验证
├─ 485 对训练图像
├─ 15 对测试图像
└─ 每对包含：低光图 + 正常光照参考图
```

#### 数据合成（核心策略）

由于 LOL 数据量较少（485对），采用物理噪声模型合成暗光数据：

```
合成流程：
正常光照图像 (FFHQ)
       │
       ▼
┌─────────────────┐
│  Inverse ISP    │  RGB → RAW（模拟传感器原始数据）
└─────────────────┘
       │
       ▼
┌─────────────────┐
│  添加噪声       │  Shot Noise (泊松) + Read Noise (高斯)
│  + 降低亮度     │  模拟低光条件
└─────────────────┘
       │
       ▼
┌─────────────────┐
│  Forward ISP    │  RAW → RGB（简化ISP处理）
└─────────────────┘
       │
       ▼
  合成的暗光图像

噪声模型公式：
N_total = Poisson(signal * gain) / gain + Gaussian(0, read_noise)
```

```python
# 核心代码参考 docs/09_数据合成.md
class LowLightSynthesizer:
    def __init__(self, iso_range=(800, 6400)):
        self.iso_range = iso_range

    def synthesize(self, img):
        # 1. 随机ISO和曝光参数
        iso = random.uniform(*self.iso_range)
        exposure_ratio = random.uniform(0.1, 0.4)

        # 2. Inverse ISP: RGB -> RAW
        raw = self.inverse_isp(img)

        # 3. 添加噪声
        noisy_raw = self.add_noise(raw, iso)

        # 4. 降低亮度
        dark_raw = noisy_raw * exposure_ratio

        # 5. Forward ISP: RAW -> RGB
        dark_img = self.forward_isp(dark_raw)

        return dark_img, img  # 返回(暗光图, 正常图)对
```

```
数据规模：
├─ 源数据：FFHQ 子集 10,000 张高清人脸
├─ 合成输出：10,000 对 (暗光图, 正常图)
└─ 增强：随机裁剪 256x256 + 翻转 + 旋转
```

#### 训练配置

```yaml
epochs: 200
batch_size: 8
optimizer: Adam
learning_rate: 1e-4
scheduler: CosineAnnealing
```

#### 导出流程

```python
# 1. 加载训练好的模型
model.load_state_dict(torch.load('best.pth'))

# 2. 关键步骤：重参数化
model.switch_to_deploy()  # 多分支 → 单分支

# 3. 导出 ONNX
dummy_input = torch.randn(1, 3, 256, 256)
torch.onnx.export(model, dummy_input, 'model.onnx', opset_version=11)
```

---

### 3.2 Tiling 分块处理（工程层核心）

#### 为什么需要 Tiling

```
问题：手机拍照 12MP (4000x3000)，直接推理会 OOM
方案：切成小块分别处理，再拼回去

Tile 大小选择：
├─ 太小 (128x128)：块太多，开销大，边界问题多
├─ 太大 (1024x1024)：单块内存占用高
└─ 推荐：512x512，平衡内存和效率
```

#### Overlap 设计

```
为什么要 Overlap：
├─ 神经网络在图像边缘的输出质量较差
├─ 直接拼接会有明显接缝
└─ Overlap 区域做融合可以消除接缝

┌─────────────────┬─────────────────┐
│     Tile 0      │overlap│  Tile 1 │
│                 │  32px │         │
└─────────────────┴───────┴─────────┘

Overlap 大小：32 像素（可调）
```

#### 融合策略

```cpp
// 线性加权融合
// 在 overlap 区域，左边 tile 权重从 1 递减到 0
// 右边 tile 权重从 0 递增到 1

for (int i = 0; i < overlap_width; i++) {
    float alpha = (float)i / overlap_width;
    output[x] = left_tile[x] * (1 - alpha) + right_tile[x] * alpha;
}
```

#### 代码结构

```cpp
class TilingProcessor {
public:
    struct Tile {
        int x, y;           // 在原图中的位置
        int width, height;  // tile 尺寸
        cv::Mat data;       // 图像数据
    };

    // 切分大图为多个 tile
    std::vector<Tile> split(const cv::Mat& input,
                            int tile_size,
                            int overlap);

    // 合并处理后的 tiles
    cv::Mat merge(const std::vector<Tile>& tiles,
                  int original_width,
                  int original_height,
                  int overlap);
};
```

---

### 3.3 线程池并行推理（工程层核心）

#### 设计思路

```
为什么不用 std::async：
├─ 每次调用可能创建新线程，开销大
├─ 无法控制并发数量
└─ 面试官会问"为什么不自己实现"

线程池优势：
├─ 线程复用，避免频繁创建销毁
├─ 可控制并发数（匹配 CPU 核心数）
└─ 任务队列解耦生产者和消费者
```

#### 参考实现

```
推荐参考：progschj/ThreadPool (GitHub)
├─ 单头文件，约 100 行
├─ 使用 C++11 标准库
├─ 代码清晰，适合学习

核心组件：
├─ std::vector<std::thread> workers    // 工作线程
├─ std::queue<std::function<void()>> tasks  // 任务队列
├─ std::mutex queue_mutex              // 保护队列
├─ std::condition_variable condition   // 线程同步
└─ std::atomic<bool> stop              // 停止标志
```

#### 关键代码

```cpp
// 1. 工作线程的循环逻辑
void worker_thread() {
    while (true) {
        std::function<void()> task;
        {
            std::unique_lock<std::mutex> lock(queue_mutex);
            // 等待任务或停止信号
            condition.wait(lock, [this] {
                return stop || !tasks.empty();
            });
            if (stop && tasks.empty()) return;
            task = std::move(tasks.front());
            tasks.pop();
        }
        task();  // 执行任务
    }
}

// 2. 提交任务
template<class F>
auto enqueue(F&& f) -> std::future<decltype(f())> {
    auto task = std::make_shared<std::packaged_task<decltype(f())()>>(
        std::forward<F>(f)
    );
    std::future<decltype(f())> res = task->get_future();
    {
        std::lock_guard<std::mutex> lock(queue_mutex);
        tasks.emplace([task]() { (*task)(); });
    }
    condition.notify_one();
    return res;
}
```

#### 与 Tiling 结合

```cpp
void process_large_image(const cv::Mat& input) {
    // 1. 切分
    auto tiles = tiling.split(input, 512, 32);

    // 2. 并行推理
    std::vector<std::future<cv::Mat>> futures;
    for (auto& tile : tiles) {
        futures.push_back(pool.enqueue([&, tile]() {
            return ncnn_inference(tile.data);  // NCNN 推理
        }));
    }

    // 3. 等待所有任务完成
    for (int i = 0; i < tiles.size(); i++) {
        tiles[i].data = futures[i].get();
    }

    // 4. 合并
    cv::Mat output = tiling.merge(tiles, input.cols, input.rows, 32);
}
```

---

### 3.4 NCNN 推理与量化（工程层）

#### NCNN 基础推理

```cpp
class NCNNInference {
private:
    ncnn::Net net;

public:
    bool load(const std::string& param_path,
              const std::string& bin_path) {
        net.opt.use_vulkan_compute = false;  // CPU 推理
        net.opt.num_threads = 1;  // 单线程（外层已有线程池）
        net.load_param(param_path.c_str());
        net.load_model(bin_path.c_str());
        return true;
    }

    cv::Mat inference(const cv::Mat& input) {
        // 预处理：BGR → RGB，归一化
        ncnn::Mat in = ncnn::Mat::from_pixels(
            input.data, ncnn::Mat::PIXEL_BGR2RGB,
            input.cols, input.rows
        );
        in.substract_mean_normalize(mean_vals, norm_vals);

        // 推理
        ncnn::Extractor ex = net.create_extractor();
        ex.input("input", in);
        ncnn::Mat out;
        ex.extract("output", out);

        // 后处理：转回 cv::Mat
        cv::Mat result = ncnn_mat_to_cv_mat(out);
        return result;
    }
};
```

#### 模型转换流程

```bash
# 1. 简化 ONNX（必须，否则 NCNN 可能转换失败）
python -m onnxsim model.onnx model_sim.onnx

# 2. 转换为 NCNN
./onnx2ncnn model_sim.onnx model.param model.bin

# 3. 优化（FP16 存储，减小模型体积）
./ncnnoptimize model.param model.bin model_opt.param model_opt.bin 65536
```

#### 量化（INT8）

```bash
# 1. 准备校准数据集（100-500 张代表性图片）
# 2. 生成量化表
./ncnn2table model_opt.param model_opt.bin calibration_images/ model.table

# 3. 量化
./ncnn2int8 model_opt.param model_opt.bin model_int8.param model_int8.bin model.table
```

#### 量化注意事项

```
可能遇到的问题：
├─ 某些层量化后精度下降严重
├─ 颜色出现偏移
└─ 边缘出现伪影

解决方案：
├─ 逐层分析，找出敏感层
├─ 敏感层保持 FP16，其他层 INT8（混合精度）
└─ 增加校准数据的多样性
```

---

### 3.6 ISP 基础模块（P1 - 展示影像基础功底）

#### 为什么需要 ISP 模块

```
目的：展示对传统影像处理的理解
├─ 面试时能讲清楚 ISP 管线原理
├─ 与 AI 增强形成互补（传统 + 深度学习）
└─ 参考 references/HDR-ISP-main/ 可快速实现

实现范围（精简版）：
├─ BLC (Black Level Correction) - 黑电平校正
├─ AWB (Auto White Balance) - 自动白平衡
└─ Demosaic - 去马赛克（Bayer → RGB）
```

#### BLC 黑电平校正

```cpp
// 参考: references/HDR-ISP-main/srcs/sources/modules/blc.cpp
// 原理：传感器在无光时也会有电信号（暗电流），需要减去

void black_level_correction(cv::Mat& raw, int black_level) {
    raw.forEach<uint16_t>([black_level](uint16_t& pixel, const int*) {
        pixel = (pixel > black_level) ? (pixel - black_level) : 0;
    });
}
```

#### AWB 自动白平衡

```cpp
// 参考: docs/03_AWB模块.md - 灰度世界法
// 原理：假设场景平均颜色为灰色，据此计算RGB通道增益

struct WBGain { float r, g, b; };

WBGain gray_world_awb(const cv::Mat& img) {
    cv::Scalar mean = cv::mean(img);
    float avg = (mean[0] + mean[1] + mean[2]) / 3.0f;

    return {
        avg / mean[2],  // R gain
        avg / mean[1],  // G gain
        avg / mean[0]   // B gain
    };
}

void apply_wb_gain(cv::Mat& img, const WBGain& gain) {
    std::vector<cv::Mat> channels;
    cv::split(img, channels);
    channels[2] *= gain.r;  // R
    channels[1] *= gain.g;  // G
    channels[0] *= gain.b;  // B
    cv::merge(channels, img);
}
```

#### Demosaic 去马赛克

```cpp
// 参考: references/HDR-ISP-main/srcs/sources/modules/demoasic.cpp
// 原理：Bayer 阵列每个像素只有一种颜色，需要插值恢复完整 RGB

// 简化版：使用 OpenCV 内置函数
cv::Mat demosaic(const cv::Mat& bayer, int pattern = cv::COLOR_BayerRG2RGB) {
    cv::Mat rgb;
    cv::cvtColor(bayer, rgb, pattern);
    return rgb;
}

// 或使用双线性插值手动实现（面试加分）
// 详见 docs/02_ISP基础.md
```

#### 项目结构

```
isp/
├── CMakeLists.txt
├── include/
│   ├── common.h          # 公共定义
│   ├── blc.h             # 黑电平校正
│   ├── awb.h             # 自动白平衡
│   └── demosaic.h        # 去马赛克
└── src/
    ├── blc.cpp
    ├── awb.cpp
    └── demosaic.cpp
```

---

### 3.7 性能测试与数据收集（验证层）

#### 需要收集的数据

```
精度指标：
├─ PSNR (Peak Signal-to-Noise Ratio)
├─ SSIM (Structural Similarity Index)
└─ 主观视觉对比图

性能指标：
├─ 推理延迟 (ms)
├─ 内存峰值 (MB)
├─ CPU 占用率 (%)
└─ 不同分辨率的表现 (720P, 1080P, 4K)

对比维度：
├─ 优化前 vs 优化后
├─ FP32 vs FP16 vs INT8
├─ 单线程 vs 多线程
└─ 不同 Tile 大小的影响
```

#### Benchmark 脚本

```cpp
void benchmark(const std::string& image_path, int iterations = 100) {
    cv::Mat input = cv::imread(image_path);

    // 预热
    for (int i = 0; i < 10; i++) {
        process(input);
    }

    // 计时
    auto start = std::chrono::high_resolution_clock::now();
    for (int i = 0; i < iterations; i++) {
        process(input);
    }
    auto end = std::chrono::high_resolution_clock::now();

    double avg_time = std::chrono::duration<double, std::milli>(end - start).count() / iterations;
    std::cout << "Average time: " << avg_time << " ms" << std::endl;
}
```

#### 最终数据表格示例

| 配置                | 1080P 延迟 | 4K 延迟 | PSNR   | 内存峰值 |
|---------------------|-----------|---------|--------|---------|
| FP32 + 单线程        | 850 ms    | 3200 ms | 23.5dB | 512 MB  |
| FP32 + 4线程 Tiling  | 280 ms    | 950 ms  | 23.5dB | 180 MB  |
| FP16 + 4线程 Tiling  | 180 ms    | 620 ms  | 23.4dB | 150 MB  |
| INT8 + 4线程 Tiling  | 95 ms     | 380 ms  | 23.1dB | 120 MB  |

---

## 四、项目文件结构

```
LowLight-Portrait-Enhancement/
├── isp/                            # P1: C++ ISP 基础模块
│   ├── CMakeLists.txt
│   ├── include/
│   │   ├── common.h
│   │   ├── blc.h                   # 黑电平校正
│   │   ├── awb.h                   # 自动白平衡
│   │   └── demosaic.h              # 去马赛克
│   └── src/
│       ├── blc.cpp
│       ├── awb.cpp
│       └── demosaic.cpp
│
├── models/                         # P0: PyTorch 模型定义
│   ├── __init__.py
│   ├── repvgg_block.py             # RepVGG Block（可重参数化）
│   ├── unet_repvgg.py              # U-Net + RepVGG 主网络
│   └── losses.py                   # L1 + Perceptual (+ Face)
│
├── train/                          # P0: 训练相关
│   ├── data_synthesis.py           # 暗光数据合成（核心）
│   ├── dataset.py                  # 数据集加载器
│   ├── train.py                    # 训练脚本
│   ├── validate.py                 # 验证脚本
│   └── configs/
│       └── train_config.yaml       # 训练配置
│
├── deploy/                         # P0: 部署相关（工程亮点）
│   ├── export_onnx.py              # PyTorch → ONNX 导出
│   ├── cpp/
│   │   ├── CMakeLists.txt
│   │   ├── include/
│   │   │   ├── tiling.h            # Tiling 分块处理
│   │   │   ├── thread_pool.h       # 线程池
│   │   │   └── ncnn_inference.h    # NCNN 推理封装
│   │   └── src/
│   │       ├── tiling.cpp
│   │       ├── thread_pool.cpp
│   │       ├── ncnn_inference.cpp
│   │       └── main.cpp            # 主程序/测试
│   ├── models/                     # 转换后的模型存放
│   │   └── .gitkeep
│   └── scripts/
│       ├── convert_ncnn.sh         # NCNN 转换脚本
│       └── quantize_int8.sh        # INT8 量化脚本
│
├── data/                           # 数据集
│   ├── LOL/                        # Low-Light 公开数据集
│   └── FFHQ_lowlight/              # 合成暗光人像数据
│       ├── low/                    # 暗光图像
│       ├── high/                   # 正常光照图像
│       └── mask/                   # Face Mask（后期添加）
│
├── benchmarks/                     # 性能测试
│   ├── benchmark.cpp
│   └── results/
│       └── performance_report.md
│
└── android/                        # P2: Android Demo（可选）
    └── .gitkeep
```

---

## 五、4 周执行计划

### Week 1：算法跑通

```
Day 1-2: 环境搭建
├─ 配置 PyTorch 环境
├─ 下载 LOL 数据集
└─ 配置 NCNN（PC 端）

Day 3-4: 模型训练
├─ 实现/调通 RepVGG U-Net
├─ 跑通训练流程
└─ 训练 100-200 epochs

Day 5-6: 模型导出
├─ 实现 switch_to_deploy()
├─ 导出 ONNX
├─ NCNN PC 端推理验证
└─ 记录 baseline 指标（PSNR、推理时间）

Day 7: 缓冲 + 复盘
└─ 确保有一个能用的模型

产出：trained_model.onnx + baseline 数据
```

### Week 2：工程核心（最重要）

```
Day 1-2: Tiling 模块
├─ 实现 split 函数（大图→小块）
├─ 实现 merge 函数（小块→大图）
├─ 实现 overlap blending
└─ 单元测试：切了再拼能还原

Day 3-4: 线程池
├─ 阅读理解 progschj/ThreadPool
├─ 自己实现一遍（或改进）
├─ 单元测试：多任务并行执行
└─ 理解 mutex、condition_variable 的作用

Day 5-6: 整合
├─ Tiling + 线程池 + NCNN 推理串起来
├─ 处理一张 4K 图，验证正确性
└─ 初步性能测试

Day 7: 调试 + 优化
├─ 解决可能的 bug（接缝、颜色不一致等）
└─ 调整 tile size、overlap、线程数

产出：能处理大图的 C++ 程序
```

### Week 3：量化 + 移动端

```
Day 1-2: 模型量化
├─ FP16 量化，测试精度和速度
├─ INT8 量化，准备校准集
└─ 对比 FP32/FP16/INT8 的精度和速度

Day 3-4: 量化调优
├─ 如果精度下降严重，分析敏感层
├─ 尝试混合精度
└─ 找到最优配置

Day 5-7: Android 部署（可选但推荐）
├─ 交叉编译 NCNN for Android
├─ 编译你的 C++ 代码为 .so
├─ 在 Android 手机上运行测试
└─ 收集真机数据

产出：量化模型 + 手机实测数据
```

### Week 4：收尾 + 面试准备

```
Day 1-2: 完整 Benchmark
├─ 多分辨率测试（720P、1080P、4K）
├─ 多配置对比（量化、线程数等）
└─ 整理成表格/图表

Day 3-4: 文档整理
├─ 记录关键设计决策
├─ 记录遇到的问题和解决方案
└─ 准备项目介绍 PPT/文档

Day 5-6: 面试话术准备
├─ 准备三套话术（影像岗、C++ 岗、通用岗）
├─ 准备常见问题的回答
└─ 准备 3-5 个"踩坑"故事

Day 7: 模拟面试
└─ 找人模拟提问，查漏补缺

产出：完整的项目 + 面试准备材料
```

---

## 六、面试准备要点

### 6.1 必须能回答的问题

#### 算法相关

```
Q: RepVGG 的重参数化是什么原理？
A: 训练时用 3x3+1x1+identity 三分支提取丰富特征，推理时利用卷积的线性性质，
   将三个分支的权重数学等价地合并为一个 3x3 卷积，减少计算量和内存访问。

Q: 为什么用 L1 Loss + Perceptual Loss？
A: L1 保证像素级准确，Perceptual Loss 用 VGG 特征匹配保证视觉质量，
   避免 L2 Loss 导致的模糊问题。

Q: PSNR 和 SSIM 分别衡量什么？
A: PSNR 衡量像素级误差，SSIM 衡量结构相似性，更符合人眼感知。
```

#### 工程相关

```
Q: 为什么要用 Tiling？
A: 12MP 图片直接推理会 OOM。Tiling 把大图切成小块，降低单次内存占用，
   还能利用多核并行加速。

Q: Overlap 区域怎么处理？
A: 用线性加权融合。在 overlap 区域，左边 tile 权重从 1 递减到 0，
   右边从 0 递增到 1，实现平滑过渡，消除接缝。

Q: 为什么不用 std::async？
A: std::async 每次可能创建新线程，开销大且并发数不可控。
   手写线程池可以复用线程，控制并发数匹配 CPU 核心数。

Q: 线程池里 condition_variable 的作用？
A: 让工作线程在没有任务时休眠，有新任务时被唤醒，避免忙等浪费 CPU。
```

#### 部署相关

```
Q: INT8 量化的原理？
A: 把 FP32 权重和激活值映射到 INT8 范围，用整数运算替代浮点运算，
   速度更快，但需要校准数据确定映射的 scale 和 zero_point。

Q: 量化后精度下降怎么办？
A: 逐层分析敏感度，敏感层（通常是首尾层）保持 FP16，其他层 INT8，
   混合精度可以在速度和精度间取得平衡。

Q: 为什么用 NCNN 不用 TensorRT？
A: 手机是 ARM 架构，没有 NVIDIA GPU。NCNN 专为移动端优化，
   支持 ARM NEON 指令集，TensorRT 只能在 NVIDIA GPU 上用。
```

### 6.2 准备的"踩坑"故事

```
故事 1：Tiling 接缝问题
"一开始直接拼接，有明显接缝。分析发现是网络边缘输出不稳定，
 加了 32 像素 overlap 和线性融合后解决。"

故事 2：量化颜色偏移
"INT8 量化后图片偏绿。定位到是第一个卷积层对量化敏感，
 改成 FP16 后颜色恢复正常。"

故事 3：多线程没加速
"加了线程池反而更慢。分析发现 tile 太小导致任务数太多，
 锁竞争严重。增大 tile size 后加速比从 0.8x 提升到 3.2x。"

故事 4：ONNX 转换失败
"NCNN 转换时报算子不支持。检查发现是某个 PyTorch 操作导出了
 不常见算子，换成等效的常见操作后解决。"
```

### 6.3 三套面试话术

#### 投递影像算法岗

> "我不只懂模型设计，我更懂端侧部署。我知道怎么把 PC 上跑通的算法，真正搬到 Android 手机上。我处理过大图分块带来的边界伪影问题，也做过 NCNN 的量化优化，在精度损失 0.4dB 的情况下实现了 2 倍加速。"

#### 投递 C++/系统开发岗

> "这个项目虽然是处理图像的，但核心是一个高性能 C++ 并行计算系统。我使用了 C++11 的 std::thread 和 std::future 实现了线程池任务调度，设计了 Tiling 策略处理大图，内存占用降低了 75%，多核加速比达到 3.2x。"

#### 投递通用应用开发岗

> "我具备全栈落地的能力。从 PyTorch 训练、ONNX 导出、NCNN 转换，到 C++ 推理引擎开发，再到 Android 端部署，整个链路我都走通了。我能独立完成从算法到产品的全流程。"

---

## 七、最终简历呈现

```
项目名称：面向移动端的高性能低光照图像增强系统

项目描述：
针对移动端低光拍摄场景，设计并实现了一套从模型训练到端侧部署的完整图像增强系统。

主要工作：
• 基于 RepVGG + U-Net 设计轻量化增强网络，利用结构重参数化技术，
  推理时将多分支合并为单分支，计算量减少 40%

• 实现 Tiling 分块策略支持 12MP 大图处理，设计 Overlap 线性融合
  消除拼接接缝，内存占用降低 75%

• 手写 C++ 线程池实现并行推理，4 核 CPU 下加速比达 3.2x

• 完成 NCNN INT8 量化部署，混合精度策略下精度损失 < 0.4dB，
  推理速度提升 2.1x

• 骁龙 8 Gen1 实测：1080P 图像处理延迟 xxx ms，达到实时处理要求

技术栈：PyTorch, NCNN, C++11, OpenCV, 多线程编程
```

---

## 八、项目总结

本项目的核心价值不在于"做了一个低光增强算法"，而在于**展示了一个完整的、可落地的移动端 AI 系统的全链路能力**。

从技术角度看，项目覆盖了三个层面：**算法层**（RepVGG 重参数化、损失函数设计）、**工程层**（C++ 线程池、Tiling 并行、内存优化）、**部署层**（NCNN 量化、移动端适配）。这种"算法 + 工程"的融合能力，正是手机厂商影像部门最看重的——他们需要的不是只会调参的"算法工程师"，而是能把算法真正落地到手机上、跑得快、占用少的"全栈工程师"。

从职业角度看，这个项目的技能组合具有很强的**可迁移性**。线程池、并行计算、性能优化这些能力，不仅适用于影像算法岗，也适用于游戏引擎、音视频处理、自动驾驶感知部署等方向。即使没有拿到影像岗的 offer，这个项目所展示的 C++ 工程能力和系统优化思维，也能支撑你去面试其他高性能计算相关的岗位。

**一个项目，三个故事，多条出路**——这就是本方案的核心策略。
